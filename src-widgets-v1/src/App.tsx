import React from 'react';
import { WidgetWebComponent } from './Widget';

export default function App(): React.JSX.Element {
    return (
        <WidgetWebComponent
            language="de"
            theme="light"
        />
    );
}
